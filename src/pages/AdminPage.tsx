// NOTE: page temporaire — la tâche 9 la remplace entièrement (chiffres clés, entonnoir, courbe…).
export function AdminPage() {
  return <div className="text-[var(--color-muted)] text-sm">Tableau de bord — en construction.</div>
}
